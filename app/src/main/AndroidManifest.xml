<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wesfood">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/inverse_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/inverse_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeSFood">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity1" />
        <activity android:name=".GetstartedActivity" />
        <activity android:name=".LoginActivity" />
       <activity android:name=".produit" />
        <activity android:name=".profil"
            android:exported="true"/>
        <activity android:name=".user" />
        <activity android:name=".tel" />






        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>